module.exports = {
    darkMode: 'class', // Enable dark mode with a class on the <html> or <body> element
    theme: {
      extend: {
        colors: {
          // Customize dark and light mode colors if needed
          darkBackground: '#1a1a1a',
          lightBackground: '#f0f0f0',
        },
      },
    },
    plugins: [],
  };
  